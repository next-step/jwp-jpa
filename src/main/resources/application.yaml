spring:
  datasource:
    url: jdbc:h2:~/test;MODE=MySQL
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create

  jooq:
    sql-dialect: org.hibernate.dialect.MySQL57Dialect

package jpa.repository;

import jpa.domain.StationDistance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StationDistanceRepository extends JpaRepository<StationDistance, Long> {

}
